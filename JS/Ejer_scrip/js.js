function calcularCirculo(Radio){
  const PI=match.PI;
  const area=PI*Match.pow(radio,2);
  const longitud=2*PI*radio;

  return{
    area:area,
    longitud: longitud
  };
}

const radio=5;
const resultado=calcularCirculo(radio);
